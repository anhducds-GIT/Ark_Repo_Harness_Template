# Ark Repo Harness

> Bộ khung để một repo tự trông coi lấy mình — cửa kiểm chặn việc dở, luật cho AI đọc, lịch bảo trì. Dựng xong thì AI là người ở lại trông nhà. 1/1 đơn vị đã khai trạng thái. Mọi con số trong repo này là máy đếm, không gõ tay.

> **SINH TỰ ĐỘNG — ĐỪNG SỬA TAY.** Sinh lại bằng `node scripts/build-dashboard.mjs`.

Trang được sinh ngày 2026-09-10.

## Việc ưu tiên #1

- **_root** — Đọc `docs/ROADMAP-V2.md` mục *Thứ tự việc — bản 10/09* TRƯỚC. Việc kế: `KHUNG-59` (hai lane chung INDEX — `git add` của người này bị `git commit` của người kia cuốn theo, đã xảy ra 2 lần trong một ngày). Hỏi trước khi vá: một `git worktree` riêng cho suite + bộ sinh có đóng được mấy trong bốn mục `KHUNG-59/50/55/51` không — chúng là MỘT bệnh.

## Đọc theo thứ tự này

- [AGENTS.md](AGENTS.md): hiến pháp repo — luật chung, đọc trước tiên
- [DASHBOARD.md](DASHBOARD.md): bảng trạng thái máy sinh: có extension gì, cái nào sống, việc đang mở
- [HANDOFF.md](HANDOFF.md): phiên gần nhất ở gốc repo làm gì, còn gì mở
- [repo-map.json](repo-map.json): bản đồ máy đọc — hệ điều phối cấp cao chỉ cần đọc file này

## Từng extension

- [ark-repo-harness](STATUS.md): active — Bốn mục đắt nhất (`KHUNG-59/50/55/51`) là MỘT bệnh: nhiều lane chung một cây làm việc git — chung đĩa, chung index, chung HEAD

## Luật bắt buộc trước khi sửa gì

- Một package chỉ MỘT phiên AI được ghi: xem `.agents/claims.json`, chủ không phải bạn thì chỉ đọc.
- Đóng phiên phải xanh cổng: `node scripts/session-check.mjs --as <tên-phiên>`.
- Push bằng `node scripts/safe-push.mjs --as <tên-phiên>`, không bao giờ `git push` trần.
