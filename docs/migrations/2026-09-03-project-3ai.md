---
kind: migration
repo: Project 3 AI Agent Unify
duong_dan: C:\WORKING ZONE\Project 3 AI Agent Unify
ngay: 2026-09-03
ban_khung: 0.3.0
nghe: điều phối nhiều AI · chủ yếu Python · trước đó tự có một bộ chuẩn đời cũ
muc_truoc: 1
muc_sau: 3
chi_phi_truoc: thả 7 · viết 12 · soi 0
chi_phi_sau: thả 0 · viết 0 · soi 0
cong_dong_phien: đỏ
trang_thai: chưa đóng được
loi_tim_ra: 8
---

## Trạng thái mới nhất

Mức 1 → **mức 3**, nợ về 0. Repo này **không hề có `package.json`** trước đó — nó là repo
Python — nên đây là lần đầu bộ khung chạm một repo khác nghề thật sự.

**Cổng đóng phiên còn 2 mục đỏ**, cả hai đều là lỗ của bộ khung chứ không phải lỗi của repo.

## Vì sao chưa đóng được

- **Phép kiểm "Bản đồ file" đóng cứng số mục `## 6.`** của bộ khung. `AGENTS.md` của repo này
  có 8 mục **không đánh số**, nên không tìm thấy bản đồ → **mọi** file mới bị coi là chưa khai.
  Nó mâu thuẫn thẳng với câu "đổi luật repo đích cho giống repo nhà là việc KHÔNG thuộc quy trình".
- 40 dòng bản đồ chưa khai được, vì `AGENTS.md` **đang bị một phiên Codex khác sửa dở**. Chạm
  vào là cuốn theo việc chưa commit của họ.

## Báo cáo đầy đủ

`drafts/MIGRATE-REPORT-3AI.md` trong chính repo đó — 8 lỗi, 5 chỗ quy trình viết sai, 9 khoản
nợ của repo đích.

Ba lỗi NẶNG đáng nhớ:

- Suite hạt giống **mâu thuẫn với chính quy trình**: quy trình bảo để `bootstrap.blocking` rỗng
  vì repo mới migrate sẽ đỏ, còn suite lại khẳng định cứng "0 đỏ". Hai cửa đo cùng một thứ, hai
  kết quả.
- `units.marker` **bắt buộc phải là JSON** mà không tài liệu nào nói. Khai một file `.md` thì
  bộ sinh bảng **chết**, còn cổng cấu trúc thì không — hai công cụ nói hai đằng.
- Cổng đóng cứng `origin/main`, trong khi repo này làm việc theo nhánh tính năng.

## Câu hỏi mở

- Repo giờ có **hai bộ chuẩn song song**: `packages/universal-project-template-v1.0` +
  `guidelines/` đời cũ, và Ark Repo Harness mới. **Cần Đức chốt** giữ cả hai hay bỏ bộ cũ.
- Phép kiểm bản đồ phải tìm theo nhãn, không theo số mục — chưa sửa.
- Ba khoá quyền vẫn đang giữ, cố ý, vì commit chưa push.
